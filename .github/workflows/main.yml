name: Session hook smoke test

on: push

jobs:
  smoke:
    name: Verify session hook
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Hook is executable
        run: test -x .claude/hooks/session-start.sh && echo "OK"
